name: cred-test

#!/usr/bin/env bash

output=$(gptscript --cache=false sys.prompt '{"message":"Please enter your fake credential.","fields":"credential"}')
credential=$(echo $output | jq -r '.credential')
echo "{\"env\":{\"MY_ENV_VAR\":\"$credential\"}}"
